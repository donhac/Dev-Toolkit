version: "3.9"

services:
  devtoolkit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devtoolkit-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
